/** @module Core */

import { Settings } from "./mRTypes";
import { MobListener } from "./DataBackend";

export default class Buff extends MobListener
{
    constructor(settings:Settings.Buff)
    {
        super();
    }
}